---
title: 面试_两个有序数组合并
date: 2017-06-02 23:24:14
tags: python
---

## 题目
```
已知两个有序数组, 现在要求将两个有序数组合并为一个有序数组.
```

## 信息
```
1. 两个有序数组是否都为升序 or 降序: 需要统一
2. 目标数组是升序 or 降序
```

## 代码实现
```
def merge_array(list_a, list_b):
    """
    :param: list_a: 有序数组a, 默认为升序
    :param: list_b: 有序数组b, 默认为升序
    :return: res: 目标数组res, 默认为升序
    """
    i, j, _res = 0, 0, list()
    a_len, b_len = len(list_a), len(list_b)

    while i < a_len and j < b_len:
        if list_a[i] < list_b[j]:
            _res.append(list_a[i])
            i += 1
        else:
            _res.append(list_b[j])
            j += 1

    if index_a == a_len:
        _res.extend(list_b[j:])
    else:
        _res.extend(list_a[i:])

    return _res
```
